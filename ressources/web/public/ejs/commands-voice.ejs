<!DOCTYPE HTML>
<html lang="en">

  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <base href="/">

    <title>fbot &middot; Commands</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.0/css/font-awesome.min.css">
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link href="css/mdb.min.css" rel="stylesheet">
    <link href="css/custom.css" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="/img/favicon.ico">

  </head>

  <body>

    <div class="wrapper">

      <div class="header">

        <a href="/" class="hidden-sm-down col-md-3 logo"></a>
        <a href="/" class="hidden-md-up col-xs-3 logo-small"></a>

      </div>

      <div class="content">

        <div class="text-xs-center">

          <h3>Voice Commands</h3><br />

          <div>
            <div class="col-md-6">
              <code>(argument)</code> => This argument is required
            </div>
            <div class="col-md-6">
              <code>argument..</code> => This argument may have whitespaces in it
            </div>
            <div class="col-md-6">
              <code>[argument]</code> => This argument is optional
            </div>
            <div class="col-md-6">
              <code>\argument</code> => This argument is admin-only
            </div>
          </div><br />

          <div class="col-xs-12">

            <hr />

            <h4>Voice commands are currently disabled due to API errors</h4>

            <hr />

            <% Object.keys(main.commands).forEach((command) => { %>

              <% if(main.commands[command].category !== 'Voice' || main.commands[command].alias) return; %>

              <code><%= main.cfg.commandPrefix + command + (main.commands[command].args ? ' ' + main.commands[command].args : '') %></code><br />
              <span><%= main.commands[command].description %></span><br />
              <small>Aliases: <%- main.commands[command].aliases ? ('<code>' + main.cfg.commandPrefix + main.commands[command].aliases.join('</code>, <code>' + main.cfg.commandPrefix) + '</code>') : 'None' %></small>
              <hr />

            <% }); %>

          </div>

        </div>

      </div>

    </div>

    <div class="footer text-xs-center">
      Copyright &copy; 2017 <a href="https://twitter.com/xmatmen">matmen</a>
    </div>

  </body>

</html>
